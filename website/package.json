{
  "name": "kirodex-website",
  "private": true,
  "scripts": {
    "dev": "tailwindcss -i style.css -o dist/style.css --watch",
    "build": "tailwindcss -i style.css -o dist/style.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
